[project]
name = "sql-vulnerable-website"
version = "0.1.0"
description = "Educational SQL injection training lab (local / supervised use)"
requires-python = ">=3.9"
dependencies = [
    "Flask==3.1.0",
    "Flask-SQLAlchemy==3.1.1",
    "SQLAlchemy>=2.0",
    "bcrypt==4.2.1",
    "python-dotenv==1.0.1",
]

[tool.vercel]
entrypoint = "app.py:app"
