services:
  sqli-lab:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_SECRET_KEY: local-dev-only-change-me
      DATABASE_URL: sqlite:////app/data/sqli_lab.db
    volumes:
      - ./data:/app/data
